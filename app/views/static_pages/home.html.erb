<% if logged_in? %>
    <div class="row">
     <p><%= t ".welcome" %></p>
    </div>
  <% else %>
  <div class="center jumbotron">
    <h1><%= t ".welcome" %></h1>
    <h2>
      <%= t ".this_is" %>
      <%= link_to t(".rails"), Settings.micropost.link_book_home %>
      <%= t ".sample_app" %>
    </h2>
    <% unless logged_in? %>
      <%= link_to t(".signup"), signup_path, class: "btn btn-lg btn-primary" %>
    <% end %>
  </div>
<% end %>
