<% if logged_in? %>
    <div class="row">
      <aside class="col-md-4">
        <section class="user_info">
          <%= render "shared/user_info" %>
        </section>
        <section class="stats">
         <%= render "shared/stats" %>
        </section>
      </aside>
        <section>
          <h3><%= t ".microposts" %> (<%= @feed_items.size %>)</h3>
          <ol class="microposts">
            <%= render @feed_items %>
          </ol>
          <%= will_paginate @feed_items %>
        </section>
    </div>
  <% else %>
  <div class="center jumbotron">
    <h3><%= t ".microposts" %> (<%= @feed_items.size %>)</h3>
      <ol class="microposts">
        <%= render @feed_items %>
      </ol>
      <%= will_paginate @feed_items %>
<% end %>
