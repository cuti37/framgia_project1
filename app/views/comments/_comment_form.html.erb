    <%= form_for ([@post, @post.comments.build], remote: true) do |f| %>
      <%= f.text_field :content, class: "form-control" %>
      <%= f.submit t(".submit"), class: "btn btn-primary send" %>
    <% end %>
